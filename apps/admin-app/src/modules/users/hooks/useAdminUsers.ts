import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'; import { usersApi } from '../api/usersApi'; import { useUsersStore } from '../store/usersStore';
export const useAdminUsers = ()=>{ const qc=useQueryClient(); const f=useUsersStore(s=>s.filters); const {data,isLoading,error}=useQuery({ queryKey:['admin-users',f], queryFn:()=>usersApi.getUsers(f) }); const toggle=useMutation({ mutationFn:(id:string)=>usersApi.toggleActivation(id), onSuccess:()=>qc.invalidateQueries({queryKey:['admin-users']}) }); return { users: data?.data??[], meta: data?.meta, isLoading, error, toggleActivation: toggle.mutate }; };